naftiko: 1.0.0-alpha2
info:
  label: Ashby API — Survey Submission
  description: 'Ashby API — Survey Submission. 2 operations. Lead operation: surveySubmission.create. Self-contained Naftiko
    capability covering one Ashby business surface.'
  tags:
  - Ashby
  - Survey Submission
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ASHBY_API_KEY: ASHBY_API_KEY
capability:
  consumes:
  - type: http
    namespace: ashby-survey-submission
    baseUri: https://api.ashbyhq.com
    description: Ashby API — Survey Submission business capability. Self-contained, no shared references.
    resources:
    - name: surveySubmission.create
      path: /surveySubmission.create
      operations:
      - name: surveysubmissioncreate
        method: POST
        description: surveySubmission.create
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: surveySubmission.list
      path: /surveySubmission.list
      operations:
      - name: surveysubmissionlist
        method: POST
        description: surveySubmission.list
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Ashby-Signature
      value: '{{env.ASHBY_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: ashby-survey-submission-rest
    port: 8080
    description: REST adapter for Ashby API — Survey Submission. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/surveysubmission-create
      name: surveysubmission-create
      description: REST surface for surveySubmission.create.
      operations:
      - method: POST
        name: surveysubmissioncreate
        description: surveySubmission.create
        call: ashby-survey-submission.surveysubmissioncreate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/surveysubmission-list
      name: surveysubmission-list
      description: REST surface for surveySubmission.list.
      operations:
      - method: POST
        name: surveysubmissionlist
        description: surveySubmission.list
        call: ashby-survey-submission.surveysubmissionlist
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ashby-survey-submission-mcp
    port: 9090
    transport: http
    description: MCP adapter for Ashby API — Survey Submission. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: surveysubmission-create
      description: surveySubmission.create
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ashby-survey-submission.surveysubmissioncreate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: surveysubmission-list
      description: surveySubmission.list
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ashby-survey-submission.surveysubmissionlist
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
