naftiko: 1.0.0-alpha2
info:
  label: Ashby API — Job Interview Plan
  description: 'Ashby API — Job Interview Plan. 1 operations. Lead operation: jobInterviewPlan.info. Self-contained Naftiko
    capability covering one Ashby business surface.'
  tags:
  - Ashby
  - Job Interview Plan
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ASHBY_API_KEY: ASHBY_API_KEY
capability:
  consumes:
  - type: http
    namespace: ashby-job-interview-plan
    baseUri: https://api.ashbyhq.com
    description: Ashby API — Job Interview Plan business capability. Self-contained, no shared references.
    resources:
    - name: jobInterviewPlan.info
      path: /jobInterviewPlan.info
      operations:
      - name: jobinterviewplaninfo
        method: POST
        description: jobInterviewPlan.info
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Ashby-Signature
      value: '{{env.ASHBY_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: ashby-job-interview-plan-rest
    port: 8080
    description: REST adapter for Ashby API — Job Interview Plan. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/jobinterviewplan-info
      name: jobinterviewplan-info
      description: REST surface for jobInterviewPlan.info.
      operations:
      - method: POST
        name: jobinterviewplaninfo
        description: jobInterviewPlan.info
        call: ashby-job-interview-plan.jobinterviewplaninfo
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ashby-job-interview-plan-mcp
    port: 9090
    transport: http
    description: MCP adapter for Ashby API — Job Interview Plan. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: jobinterviewplan-info
      description: jobInterviewPlan.info
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ashby-job-interview-plan.jobinterviewplaninfo
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
