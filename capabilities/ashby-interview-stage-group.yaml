naftiko: 1.0.0-alpha2
info:
  label: Ashby API — Interview Stage Group
  description: 'Ashby API — Interview Stage Group. 1 operations. Lead operation: interviewStageGroup.list. Self-contained
    Naftiko capability covering one Ashby business surface.'
  tags:
  - Ashby
  - Interview Stage Group
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ASHBY_API_KEY: ASHBY_API_KEY
capability:
  consumes:
  - type: http
    namespace: ashby-interview-stage-group
    baseUri: https://api.ashbyhq.com
    description: Ashby API — Interview Stage Group business capability. Self-contained, no shared references.
    resources:
    - name: interviewStageGroup.list
      path: /interviewStageGroup.list
      operations:
      - name: interviewstagegrouplist
        method: POST
        description: interviewStageGroup.list
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Ashby-Signature
      value: '{{env.ASHBY_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: ashby-interview-stage-group-rest
    port: 8080
    description: REST adapter for Ashby API — Interview Stage Group. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/interviewstagegroup-list
      name: interviewstagegroup-list
      description: REST surface for interviewStageGroup.list.
      operations:
      - method: POST
        name: interviewstagegrouplist
        description: interviewStageGroup.list
        call: ashby-interview-stage-group.interviewstagegrouplist
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ashby-interview-stage-group-mcp
    port: 9090
    transport: http
    description: MCP adapter for Ashby API — Interview Stage Group. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: interviewstagegroup-list
      description: interviewStageGroup.list
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ashby-interview-stage-group.interviewstagegrouplist
      outputParameters:
      - type: object
        mapping: $.
