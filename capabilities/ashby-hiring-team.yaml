naftiko: 1.0.0-alpha2
info:
  label: Ashby API — Hiring Team
  description: 'Ashby API — Hiring Team. 2 operations. Lead operation: hiringTeam.addMember. Self-contained Naftiko capability
    covering one Ashby business surface.'
  tags:
  - Ashby
  - Hiring Team
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ASHBY_API_KEY: ASHBY_API_KEY
capability:
  consumes:
  - type: http
    namespace: ashby-hiring-team
    baseUri: https://api.ashbyhq.com
    description: Ashby API — Hiring Team business capability. Self-contained, no shared references.
    resources:
    - name: hiringTeam.addMember
      path: /hiringTeam.addMember
      operations:
      - name: hiringteamaddmember
        method: POST
        description: hiringTeam.addMember
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: hiringTeam.removeMember
      path: /hiringTeam.removeMember
      operations:
      - name: hiringteamremovemember
        method: POST
        description: hiringTeam.removeMember
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Ashby-Signature
      value: '{{env.ASHBY_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: ashby-hiring-team-rest
    port: 8080
    description: REST adapter for Ashby API — Hiring Team. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/hiringteam-addmember
      name: hiringteam-addmember
      description: REST surface for hiringTeam.addMember.
      operations:
      - method: POST
        name: hiringteamaddmember
        description: hiringTeam.addMember
        call: ashby-hiring-team.hiringteamaddmember
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/hiringteam-removemember
      name: hiringteam-removemember
      description: REST surface for hiringTeam.removeMember.
      operations:
      - method: POST
        name: hiringteamremovemember
        description: hiringTeam.removeMember
        call: ashby-hiring-team.hiringteamremovemember
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ashby-hiring-team-mcp
    port: 9090
    transport: http
    description: MCP adapter for Ashby API — Hiring Team. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: hiringteam-addmember
      description: hiringTeam.addMember
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ashby-hiring-team.hiringteamaddmember
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: hiringteam-removemember
      description: hiringTeam.removeMember
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ashby-hiring-team.hiringteamremovemember
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
