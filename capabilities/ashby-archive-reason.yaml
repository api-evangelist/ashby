naftiko: 1.0.0-alpha2
info:
  label: Ashby API — Archive Reason
  description: 'Ashby API — Archive Reason. 1 operations. Lead operation: archiveReason.list. Self-contained Naftiko capability
    covering one Ashby business surface.'
  tags:
  - Ashby
  - Archive Reason
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ASHBY_API_KEY: ASHBY_API_KEY
capability:
  consumes:
  - type: http
    namespace: ashby-archive-reason
    baseUri: https://api.ashbyhq.com
    description: Ashby API — Archive Reason business capability. Self-contained, no shared references.
    resources:
    - name: archiveReason.list
      path: /archiveReason.list
      operations:
      - name: archivereasonlist
        method: POST
        description: archiveReason.list
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Ashby-Signature
      value: '{{env.ASHBY_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: ashby-archive-reason-rest
    port: 8080
    description: REST adapter for Ashby API — Archive Reason. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/archivereason-list
      name: archivereason-list
      description: REST surface for archiveReason.list.
      operations:
      - method: POST
        name: archivereasonlist
        description: archiveReason.list
        call: ashby-archive-reason.archivereasonlist
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ashby-archive-reason-mcp
    port: 9090
    transport: http
    description: MCP adapter for Ashby API — Archive Reason. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: archivereason-list
      description: archiveReason.list
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ashby-archive-reason.archivereasonlist
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
